# 2026-07-13 工作记录

## 认证：新增登出接口
- `AuthController`（platform-admin）新增 `POST /auth/logout`，调用 `StpUtil.logout()` 注销当前 Sa-Token 会话。
- 全项目此前无 logout 用法先例；登录用 `StpUtil.login(sysUser.getId())`，token 名为 Sa-Token 默认 `satoken`。
- 接口风格与 `login` 一致（`@Operation` + `@PostMapping`），import `StpUtil` 已存在。
- 编译验证：`mvn compile -pl platform-admin -am -DskipTests` 通过。

## common：新增 event 包与用户缓存删除事件
- 新建 `com.platform.common.event` 包；新增 `UserCacheDeleteEvent extends ApplicationEvent`。
- 携带 `userId`（缓存 key 维度 `user:auth:perm:{userId}` / `user:auth:role:{userId}`）+ `username`（日志辅助）。
- 项目此前无 Spring 事件先例，采用标准 `ApplicationEvent` 方式，由 `ApplicationEventPublisher` 发布、`@EventListener` 消费。
- 编译验证：`mvn compile -pl platform-common -DskipTests` 通过。

## /user/info 接口加 Redis 缓存
- 新增缓存 key `RedisConstant.USER_INFO = "user:info:"`（后接 userId）。
- `SysUserController#info()` 改为 cache-aside：先读 `user:info:{userId}`，命中用 `JacksonUtil.parseObj(..., UserInfoVO.class)` 返回；未命中查库聚合后 `redisUtil.set` 写回（TTL 全局默认）。
- 约束沿用之前菜单树经验：UserInfoVO 在 admin 包、component 无法引用，故 cache-aside 放 Controller 层，失效逻辑放 UserComponent（只删 redis key，不依赖 admin 类型）。
- 失效：`UserComponent.cleanUserCache` / `cleanUserCacheAndSession` 两方法追加删除 `USER_INFO+userId`，覆盖 edit/delete/editStatus 等写路径。
- 编译验证：`mvn compile -pl platform-admin -am -DskipTests` 通过。

## /user/info 数据聚合+缓存下沉到 UserComponent
- 新建 common 层聚合载体 `com.platform.common.bo.UserInfoBO`（含 SysUser user / List<SysRole> roles / List<String> permissions / List<SysMenu> menus 扁平列表），作为跨模块传递+缓存的 entity 级对象。
- `UserComponent` 注入 `SysUserService` + `SysRoleService`，新增 `getUserInfo(userId)`：cache-aside（读 `user:info:{id}` → 未命中 findById+listRoleById+listMenuByIds 聚合、perms 去重 → 写回），返回 UserInfoBO。
- `SysUserController#info()` 瘦身为纯展示层：调 `userComponent.getUserInfo(id)` 拿 BO，再转 UserVO/RoleVO/MenuTreeVO 树 + UserInfoVO 返回。VO 转换因模块边界（component 不能引用 admin VO）无法下沉，与 MenuComponent#getMenuTree 处理方式一致。
- 编译验证：EXIT=0 通过。

## UserComponent 接入 UserCacheDeleteEvent
- `UserComponent` 注入 `ApplicationEventPublisher`，新增 `@EventListener onUserCacheDeleteEvent(UserCacheDeleteEvent)`：统一删除 `USER_AUTH_PERM` / `USER_AUTH_ROLE` / `USER_INFO` 三个 key（key 维护集中到监听器，调用方无需感知）。
- `cleanUserCache` / `cleanUserCacheAndSession` 改造为在 `asyncManager.execute(...)` 异步线程中 `publishEvent(new UserCacheDeleteEvent(this, userId))`；踢下线（kickout）仍在 `cleanUserCacheAndSession` 内先执行。异步行为与改造前一致（监听器随发布线程同步执行，跑在 asyncManager 线程）。
- 注意：`UserCacheDeleteEvent` 实际构造签名只有 `(Object source, Long userId)`（无 username 参数），memory 旧记的 username 字段不实。
- 编译验证：缺 `@EventListener` import 报一次错，补 `org.springframework.context.event.EventListener` 后 EXIT=0 通过。

## getUserInfo 内直接构建菜单树
- 需求：菜单树在 `UserComponent#getUserInfo` 内构建好（不再由 Controller 建树）。
- 约束：component 不能引用 admin 的 MenuTreeVO，故树必须用 entity 级 `SysMenu` 节点构建。
- 改动：
  - `SysMenu`（common 实体）新增 transient 字段 `children: List<SysMenu>`（`@TableField(exist = false)`），并补 `import java.util.List;`。
  - `UserComponent#getUserInfo` 末步调用私有递归 `buildMenuTree(List<SysMenu>, 0L)`（与菜单管理 SysMenuController 算法一致）把 `menus` 从扁平列表构建为树，BO 的 `menus` 现在承载树。缓存 JSON 含嵌套 children，Jackson 可正常反序列化。
  - `SysUserController#info()` 新增私有 `convertMenuTree(List<SysMenu>)`：把实体树递归转成 `MenuTreeVO` 树并补全 `statusText`/`menuTypeText`（展示层转换仍留 admin，因依赖 VO）。
- 未复用 `TreeBuilderComponent`：其 `TreeNode<T>` 接口要求 `List<TreeNode<T>>` 子类，与 `SysMenu.children: List<SysMenu>` 泛型不匹配，强行实现需裸强转，故自写递归建树。
- 编译验证：common 缺 `import java.util.List` 报一次错，补后 EXIT=0 通过。

## getUserInfo 改回：撤销 SysMenu.children，UserInfoBO 用内部 BO 类
- 用户反馈：在 `SysMenu` 实体加 `children` 字段「破坏实体结构」，要求改回去。
- 新方案：`UserInfoBO` 不再引用 entity，改为三个内部 static BO 类承载：`UserBO`（镜像 sys_user 展示字段，含 postIds/roleIds 字符串）、`RoleBO`（镜像 sys_role）、`MenuBO`（镜像 sys_menu，扁平、无 children）。`menus` 恢复为 `List<MenuBO>` 扁平列表。
- 还原：`SysMenu` 删除 `children` 字段及 `import java.util.List;`。
- `UserComponent#getUserInfo`：移除 `buildMenuTree`（实体树），改为用 `MapperFacade`（starter 的 `@AutoConfiguration` bean，随主应用组件扫描进同一上下文，component 可注入）把 entity → BO；`menus` 返回扁平 `List<MenuBO>`。
- `SysUserController#info()`：展示层基于扁平 `MenuBO` 建树——新增 `toUserVO/toRoleVO/toMenuTreeVO`（BO→VO，补 statusText/menuTypeText；toUserVO 还把 postIds/roleIds 拆成 pIds/rIds，与 view() 一致）+ `buildMenuTree(List<MenuTreeVO>, 0L)`（与菜单管理算法一致）。补 `import com.platform.admin.vo.role.RoleVO;`。
- 注意：component 注入 `MapperFacade` 首次在此模块使用，已验证可用（编译 EXIT=0）。
- 编译验证：controller 缺 `RoleVO` 显式 import（vo.role 子包，非 user.* 通配覆盖）报一次错，补后 EXIT=0 通过。

## getUserInfo 菜单树改回在组件层构建（MenuBO 带 children）
- 用户再次要求：菜单树直接在 `UserComponent#getUserInfo` 里构建，把 `children` 加在 `MenuBO`（BO，不污染实体）上。
- 改动：
  - `UserInfoBO.MenuBO` 新增 `children: List<MenuBO>` 字段。
  - `UserComponent#getUserInfo`：先把 entity 映射为 `List<MenuBO>` 扁平列表，再调用私有 `buildMenuTree(List<MenuBO>, 0L)`（与菜单管理算法一致）构建树，`bo.setMenus(...)` 承载树；缓存 JSON 含嵌套 children，Jackson 可反序列化。
  - `SysUserController#info()`：移除展示层 `buildMenuTree`，改为递归 `convertMenuTree(List<MenuBO>)` 把组件层建好的 BO 树转成 `MenuTreeVO` 树（补 statusText/menuTypeText）。`toMenuTreeVO` 仍逐节点转换。
- 结论：`children` 字段放在 BO 上可以接受（不污染 entity），与之前在 `SysMenu` 实体上加 children 被否不同。
- 编译验证：EXIT=0 通过。

## 新增 AllUserCacheDeleteEvent（所有用户缓存删除事件）
- 需求：加一个「所有用户缓存删除事件」。
- 新增 `com.platform.common.event.AllUserCacheDeleteEvent extends ApplicationEvent`（无参数据，`super(source)`）。
- `UserComponent` 新增 `@EventListener onAllUserCacheDeleteEvent(AllUserCacheDeleteEvent)`：用 `redisUtil.keys(prefix + "*")` 匹配三个前缀 `USER_AUTH_PERM` / `USER_AUTH_ROLE` / `USER_INFO`，再 `redisUtil.delete(keys)` 批量删（keys 返回去前缀 key，delete 会再 wrap，配套正确）；新增发布方法 `cleanAllUserCache()`（asyncManager 异步 `publishEvent(new AllUserCacheDeleteEvent(this))`）。
- **连带修复三处通配符 hack bug**：`MenuComponent.cleanMenuCache()`、`RoleComponent.cleanRoleCache()`、`AuthController.logout()` 原来传 `new UserCacheDeleteEvent("*")` 或 `String.valueOf(userId)` 给 `UserCacheDeleteEvent(Long userId)`（String→Long 编译失败；且 `"*"` 拼成字面量 key `user:auth:perm:*` 也删不掉真实缓存）。前两处改为发布 `AllUserCacheDeleteEvent(this)`（语义：清所有用户缓存）；`AuthController.logout()` 改为 `new UserCacheDeleteEvent(userId)`（登出只清当前用户）。
- 编译验证：修完三处后 EXIT=0 通过。
- 重要约定：`UserCacheDeleteEvent` 构造器为 `(Long userId)`，调用方必须传 Long 而非 String；凡「清所有用户」语义应改用 `AllUserCacheDeleteEvent`。

## 用户缓存删除事件加日志
- `UserComponent` 两个监听器（`onUserCacheDeleteEvent` / `onAllUserCacheDeleteEvent`）加 `@Slf4j` 日志：单用户事件记 `userId`；所有用户事件记前缀清除动作，并捕获 `redisUtil.delete(keys)` 返回值（删除 key 数）一并输出。
- 编译验证：EXIT=0 通过。

## 新增系统监控模块（ServerMonitor）
- 需求：类似 RuoYi 服务监控，采集 CPU/内存/JVM/磁盘/Redis 指标，**不需要建表**（纯运行时采样）。
- **依赖**：根 pom.xml 新增 `<oshi.version>6.8.2</oshi.version>` 及 `oshi-core` 依赖管理；`platform-component/pom.xml` 引入 `oshi-core`。
- **RedisUtil**（starter）：新增 `getInfo()` 方法，通过 `RedisCallback<Properties>` 执行 Redis `INFO` 命令返回属性。
- **ServerBO.java**（common/bo）：监控聚合载体，含内部 static 类 `Cpu/Mem/Jvm/Sys/SysFile/RedisCache`。
- **ServerComponent.java**（component/monitor）：OSHI 采集 CPU（tick 采样）/内存；Runtime+ManagementFactory 采集 JVM；FileSystem 采集磁盘；RedisUtil.getInfo() 采集 Redis。组装 ServerBO 返回。
- **ServerController.java**（admin/controller/monitor）：`GET /monitor/server` 调 component 直接返回 `Result<ServerBO>`。
- oshi API 注意事项：6.8.2 没有 `getProcessCpuLoadBetweenTicks()` 和 `OperatingSystem#getProcessCpuLoad()`，进程 CPU 直接复用系统 CPU 值。
- 编译验证：`RedisCallback` 包名修正为 `org.springframework.data.redis.core` 后 EXIT=0 通过（BUILD SUCCESS）。

## 系统监控新增 RabbitMQ 信息
- 项目用 RabbitMQ（`spring-boot-starter-amqp`，3.1.4 实际解析版本；pom 写 3.2.5 但本地仓库只有 3.1.4）。
- **ServerBO** 新增 `MqCache mq` 字段 + 内部 static 类（name/version/host/port/clusterName/status/queueCount/messageCount/consumerCount）。
- **ServerComponent** 注入 `RabbitTemplate`，新增 `setMqInfo()`：连接信息从 `rabbitTemplate.getConnectionFactory().createConnection().getDelegate()`（原生 `com.rabbitmq.client.Connection`）取 `serverProperties`（version/cluster_name）、`getAddress()`/`getPort()`/`isOpen()`；队列统计遍历 `RabbitMqConstant.USER_QUEUE`/`DEAD_LETTER_QUEUE`，用 `rabbitTemplate.execute(channel -> channel.queueDeclarePassive(name))` 拿 `DeclareOk` 的 `getMessageCount()`/`getConsumerCount()`。单队列异常不致命（内层 try-catch）。
- **关键坑（amqp 3.1.4）**：`RabbitAdmin.getQueueNames()` 与 `QueueInformation` 类都不存在（新版才有），故不能遍历全部队列，改用项目声明队列常量 + 原生 `queueDeclarePassive`。`RabbitAdmin` 无需注入。
- 编译验证：EXIT=0 通过（BUILD SUCCESS）。

## 磁盘信息新增 GB 换算字段
- `ServerBO.SysFile` 新增 `totalGb/usedGb/freeGb`（double，保留两位小数）。
- `ServerComponent#setSysFiles` 按 `字节 / 1024.0 / 1024.0 / 1024.0` 换算填入；原字节字段保留。
- 编译验证：`mvn compile -pl platform-admin -am -DskipTests` 通过（BUILD SUCCESS）。

## sql 目录曾被清空，已重建 + 定时任务菜单
- 发现 `sql/` 目录为空（`menu_init.sql` / `sys_job_file.sql` 均不存在，目录时间戳 20:09 疑似被清空）。
- 已按上下文原内容重建两文件，避免丢失菜单与表定义。
- `menu_init.sql` 新增「定时任务」(id 53，父 系统管理) + 按钮(新增/修改/删除/任务状态/立即执行) 与「任务日志」(id 59，父 监控管理) + 按钮(删除/清空)；`AUTO_INCREMENT` 改 62，清空区间注释改 1~61。
- `sys_job_file.sql` 含 `sys_job` / `sys_job_log` / `sys_file` 三表 + 初始化数据，对接 Quartz 定时任务模块。
- 定时任务模块分层约定：实体放 common、Mapper/Service 放 service、`ScheduleComponent` 放 component、Controller/VO 放 admin；`status`(1正常/0暂停) 驱动 Quartz 注册/暂停。

## 用户要求撤销定时任务模块（删除两表+菜单）
- `sql/sys_job_file.sql`：删除 `sys_job`、`sys_job_log` 两张表（建表 + 初始化数据），仅保留 `sys_file`；头部注释及文件管理段序号一并清理（"3."改"1."）。
- `sql/menu_init.sql`：删除「定时任务」(id 53~58) 与「任务日志」(id 59~61) 菜单及按钮；`AUTO_INCREMENT` 回退 53，清空区间注释回退 1~52。
- 结论：当前未写任何定时任务 Java 代码，仅 SQL 层被撤回，无代码改动需回滚。

## 删除全部 sql 脚本
- 用户要求删除 `sql/` 下所有脚本，已 `rm -f` 移除 `menu_init.sql` 与 `sys_job_file.sql`。
- 当前 `sql/` 目录为空，无任何建表/菜单初始化 SQL。后续若需建表/初始化数据需重新生成。

## 重新生成定时任务表（仅任务相关）
- 用户要求重新生成定时任务表，仅保留定时任务相关表（不含 sys_file、不含菜单）。
- 新建 `sql/sys_job.sql`：`sys_job`（任务定义）+ `sys_job_log`（执行日志）两张表及初始化数据（沿用 RyTask 示例，id 1/2 status=0、id 3 status=1）。
- 字段对齐 RuoYi：`invoke_target` / `cron_expression` / `misfire_policy` / `concurrent` / `status`。

## sys_job 两表 id 改为数据库自增
- 用户确认 `sys_job`、`sys_job_log` 的 `id` 改为 `BIGINT NOT NULL AUTO_INCREMENT`（建表 SQL 已改，初始化数据手写 id 仍可插入）。
- 配套待改：实体 `@TableId("id")` 需加 `type = IdType.AUTO`，否则 MyBatis-Plus 默认 ASSIGN_ID 雪花会与自增冲突，自增不生效。

## sys_job.misfire_policy 注释澄清歧义
- SQL 中 `misfire_policy` COMMENT 改为 `'错失触发策略(1补跑,2/3不补跑)'`，字段上方加 `--` 注释说明 1/2/3 与 Quartz 映射（2/3 底层均 DO_NOTHING，仅业务语义不同）。
- 用户最终拍板：`misfire_policy` 简化为**两个值** `0=不补跑(默认)` / `1=补跑`，删掉冗余的 2/3；示例数据 misfire_policy 由 3 改为 0。建表与初始化数据均已同步。

## 定时任务模块后端生成（Quartz + RAMJobStore）— BUILD SUCCESS
- 依赖：`platform-component` pom 引入 `spring-boot-starter-quartz`（Spring Boot BOM 管理版本）+ `platform-service`。
- common 枚举：`JobStatusEnum`(0暂停/1正常)、`MisfirePolicyEnum`(0不补跑/1补跑)、`ConcurrentEnum`(0禁止/1允许)、`JobLogStatusEnum`(0失败/1成功)。
- 实体：`SysJob`/`SysJobLog` 的 `@TableId` 改 `type=IdType.AUTO` 匹配 AUTO_INCREMENT 建表。
- service：`SysJobService`/`SysJobLogService` 补全 findById/paging/selectList/listNormalRunningJobs/clean；`SysJobLogServiceImpl.clean` 用 `remove(new LambdaQueryWrapper<>())`（MyBatis-Plus 的 `Wrappers` 在 `core.toolkit` 包，本项目直接用 LambdaQueryWrapper）。
- component(`com.platform.component.job`)：`ScheduleUtils`(JobDetail/Trigger，concurrent 选 DisallowConcurrentExecution 类，misfire 映射 FireAndProceed/DoNothing)、`QuartzJobExecution`(implements Job，非 Spring Bean 用 Hutool SpringUtil 取依赖，查库+反射调用+写日志)、`QuartzDisallowConcurrentExecution`(@DisallowConcurrentExecution)、`JobInvokeUtil`(解析 beanName.method(args) 反射，兼容基本类型)、`ScheduleComponent`(createScheduleJob/pauseJob/resumeJob/deleteJob/runOnce/initAllRunningJobs)。
- admin：`SysJobController`(/page/select-list/view/enums/add/edit/delete/changeStatus/run，cron 用 `CronExpression.isValidExpression` 校验，SchedulerException 捕获抛 BusinessException)、`SysJobLogController`(/page/view/delete/clean)、VO(SysJobVO/SaveVO/EditVO/LogVO)、`RyTask`(@Component("ryTask") 示例 Bean)、`JobInitRunner`(ApplicationRunner 启动恢复 status=1)。
- `/enums` 因三枚举返回 `Map<String,List<EnumVO>>`（偏离 skill 单 List 规范，工程需要）。
- 编译：全模块 BUILD SUCCESS（mvn compile -pl platform-admin -am）。

## 任务日志关联 jobId（取代 jobName/jobGroup 匹配）
- `sys_job_log` 表新增 `job_id BIGINT NOT NULL` 字段 + `idx_job_id` 索引；初始化数据补 job_id。
- `SysJobLog` 实体加 `jobId` 字段；`SysJobLogVO` 加 `jobId`。
- `QuartzJobExecution.buildLog` 写日志时 `setJobId(job.getId())`。
- `SysJobLogController./page` 直接 `paramsMap.put("jobId", jobId)`，移除 SysJobService 反查与注入。
- `SysJobLogServiceImpl.paging` 用 `eq(SysJobLog::getJobId, jobId)` 精确过滤，保留 keyword 模糊与 status。
- 优点：任务改名也能查历史日志（jobId 不变）。
- 编译 BUILD SUCCESS。

## 启动 banner
- 用户要的是 Spring Boot 启动控制台 banner（非图片）。
- 生成 `platform-admin/src/main/resources/banner.txt`：ASCII art "AI" + 中文标语「AI 赋能，前端飞升」+ `${spring-boot.version}` 占位符。Spring Boot 自动识别 classpath:banner.txt 启动时打印。
- 删除之前误生成的 `static/banner.svg` 及空 static 目录。

## 并发策略测试支持
- `RyTask` 加 `longTask()`：sleep 5s + 打印线程名/开始结束时间，用于测试 concurrent 字段。
- 新建 `sql/job_concurrent_test.sql`：两条测试任务（A: concurrent=1 status=1 启动即跑；B: concurrent=0 status=0 默认暂停），invokeTarget=ryTask.longTask，cron `0/2 * * * * ?`。执行 SQL + 重启应用，JobInitRunner 自动注册 status=1 的任务。
- 验证点：concurrent=1 多实例重叠（不同 worker 线程）；concurrent=0 串行跳过（开始/结束配对，中间触发点无输出）。

## 主键策略改用数据库自增（项目级约定）
- `application.yml` 加 `mybatis-plus.global-config.db-config.id-type=AUTO`，全局生效。
- 所有实体 `@TableId("id")` 走全局默认（无需逐个改）；SysJob/SysJobLog 显式 `IdType.AUTO` 一致保留。
- 硬前提：所有表 id 列必须 `AUTO_INCREMENT`，否则 insert 报错。项目建表 SQL 仅 `sql/sys_job.sql`（已 AUTO），其他表（sys_user/sys_role/sys_menu 等）需手动 `ALTER TABLE xxx MODIFY id BIGINT NOT NULL AUTO_INCREMENT;`。
- 历史雪花 id 数据保留不影响；新 insert 自增从 max(id)+1。