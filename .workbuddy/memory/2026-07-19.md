# 2026-07-19 工作日志

## 1. 登录/二次认证接入 RSA 非对称加密（后端）
- 背景：用户决定「HTTPS 由网关终结、后端只做 RSA 应用层兜底」。密码存储层 BCrypt 不变，传输层加 RSA。
- 新增文件（platform-starter/.../security）：
  - `RsaComponent.java`：构造时生成 RSA 2048 密钥对，私钥仅驻留内存（不落盘/不进仓库/不打日志）；`getPublicKeyBase64()` 返回 X.509 DER 的 Base64；`decryptByPrivateKey(String)` 用 `RSA/ECB/PKCS1Padding` 解密（与前端 jsencrypt 默认算法一致）。使用 java.util.Base64，无额外依赖。
  - `RsaAutoConfiguration.java`：`@Bean RsaComponent`（@ConditionalOnMissingBean），注册进 `META-INF/spring/.../AutoConfiguration.imports`（新增第 7 个自动配置）。
- 改动（platform-admin）：
  - `vo/LoginVO.java`、`vo/SecondAuthVO.java`：各加 `Boolean encrypted = false`（默认 false，保证 Swagger/curl 明文调用仍可用）。
  - `controller/AuthController.java`：注入 `RsaComponent`；新增 `GET /auth/rsa/public-key` 返回公钥 Base64；`login()` 与 `secondAuth()` 在 encrypted=true 时用私钥解密后再走 BCrypt matches。
- 验证：`mvn -o -pl platform-admin -am compile` BUILD SUCCESS。
- 关键约束（告知前端）：① 公钥接口返回的是裸 Base64，jsencrypt 需先包 `-----BEGIN PUBLIC KEY-----` / `-----END PUBLIC KEY-----` PEM 头尾才能 setPublicKey；② 每次启动密钥对重建，前端必须「登录前现取公钥」再加密，不能写死；③ 分别加密 username、password（各 ≤245 字节，够用）；④ 仅 RSA 无 HTTPS 时 token 仍明文，本方案依赖网关 HTTPS 兜底。
- 用户安全决策（记 MEMORY.md）：本项目传输安全 = 网关 HTTPS（终结 TLS，后端零配置）+ 后端 RSA 应用层兜底；密码存储 BCrypt 不变。

## 2. 移除 encrypted 判断，改为无条件加解密
- 用户决定：前端必须用 RSA 加密、后端无条件解密，不需要 `encrypted` 标志位判断。
- `LoginVO` / `SecondAuthVO` 删除 `Boolean encrypted` 字段。
- `AuthController.login()`：`username/password` 直接 `rsaComponent.decryptByPrivateKey(...)`（不再包 if）。
- `AuthController.secondAuth()`：`password` 直接解密。
- 副作用：Swagger/curl 明文登录不再可用（解密会失败），符合「强制加密」预期。`GET /auth/rsa/public-key` 仍保留供前端取公钥。

## 3. sys_attachment 字段 biz_type / biz_id 改类型 + 附件类型枚举
- 需求：biz_type 改 tinyint（对应后端枚举「附件类型」），biz_id 改 bigint；生成迁移 SQL。
- 新增枚举 `platform-common/.../enums/AttachmentBizTypeEnum.java`：code 1=头像 2=文章图片 3=文档附件 4=导入模板 5=其他；含 getByCode/getDescByCode/fromCode（风格对齐 StorageTypeEnum）。
- 类型改动：
  - `SysAttachment` 实体：`bizType` String→Integer，`bizId` String→Long。
  - `AttachmentVO`：`bizType`→Integer、`bizId`→Long，新增 `bizTypeDesc`（后端按 code 填充含义，供前端展示）。
  - `SysAttachmentController`：`upload`/`page` 的 bizType/bizId 入参改 Integer/Long；upload/view/page 返回 VO 前 `vo.setBizTypeDesc(AttachmentBizTypeEnum.getDescByCode(...))`；page 的 paramsMap 直接放 Integer（去掉 StrUtil.trim）。
  - `FileChunkController.merge`：bizType/bizId 入参改 Integer/Long，返回 VO 同样填 bizTypeDesc。
  - `SysAttachmentServiceImpl.paging`：`bizType` 从 paramsMap 取为 Integer，过滤条件由 `nonNull && !isBlank()` 改为仅 `nonNull`（Integer 无 isBlank）。
- SQL：
  - 新增 `sql/sys_attachment_alter.sql`（ALTER TABLE MODIFY COLUMN，含「历史字符串值先 UPDATE 成枚举 code」提示）。
  - 同步更新 `sql/platform_db.sql` 中 sys_attachment 的 CREATE TABLE 两列类型与注释。
- 验证：admin `mvn -o -pl platform-admin -am compile` BUILD SUCCESS。
- 影响：上传/分片/列表/详情 接口 bizType 入参由字符串变数字 code；前端调附件接口需按 AttachmentBizTypeEnum 传 code，不再传 'avatar' 等字符串。

## 4. sys_user.avatar 改 bigint（存头像附件 ID）
- 需求：将用户头像字段由"头像URL字符串"（varchar(255)）改为 bigint，存 sys_attachment.id（0=未设置），与前面"头像走附件接口"方案对齐。
- 类型改动：
  - `SysUser` 实体：`avatar` String→Long（注释「头像附件ID，关联 sys_attachment.id，0=未设置」）。
  - `UserVO` / `UserSaveVO` / `UserEditVO`：`avatar` String→Long，Schema 注释/示例同步改（example 改 "0"）。
  - `UserInfoBO`（common，登录返回给前端的用户信息）：`avatar` String→Long。
  - 映射：VO/BO 经 mapperFacade(Orika) 按字段名自动映射，改类型无额外代码。
- SQL：
  - 新增 `sql/sys_user_alter.sql`：先 `UPDATE sys_user SET avatar=0 WHERE 含非数字/空/NULL`（兼容历史 '/avatar/*.png' 字符串，避免 MODIFY 类型转换报错），再 `ALTER TABLE MODIFY COLUMN avatar bigint NOT NULL DEFAULT 0`。含「旧 URL→真实附件ID 可选迁移」说明。
  - 同步更新 `sql/platform_db.sql`：sys_user CREATE TABLE 的 `avatar` 列改为 bigint DEFAULT 0；5 条种子 INSERT 的 avatar 值由 '/avatar/*.png' 改为 0。
- 验证：admin `mvn -o -pl platform-admin -am compile` BUILD SUCCESS（仅无关 TreeBuilderComponent 的 unchecked 提示）。
- 前端契约：user 相关接口 avatar 字段由字符串 URL 变数字（附件ID）；展示时 avatar!=0 调 `/attachment/preview?id={avatar}`，avatar=0 用系统默认头像（sys_config 的 sys.user.avatar 配置项）。

## 5. sys_user.avatar 字段改名 avatar_id（列名+Java 字段）
- 需求（接上条）：字段语义是"附件ID"，命名应自解释，故把 `avatar` 改名为 `avatar_id`（列名）+ `avatarId`（Java 字段），与现有 `biz_id`/`config_id`/`dept_id` 的 `_id` 风格一致。
- 实体/VO/BO 改动（均 String/Long 已就位，仅改字段名）：
  - `SysUser`：`@TableField("avatar")`→`@TableField("avatar_id")`，`private Long avatar;`→`private Long avatarId;`（@Setter@Getter 自动生成 getAvatarId/setAvatarId）。
  - `UserVO`/`UserSaveVO`/`UserEditVO`：`private Long avatar;`→`private Long avatarId;`。
  - `UserInfoBO`（common 嵌套类）：同步 `avatarId`。
- SQL：
  - `sql/platform_db.sql` CREATE TABLE：`avatar` 列名→`avatar_id`（类型/注释/默认值不变）。5 条种子 INSERT 用位置赋值无列名，列名变更不影响，仍保持 0。
  - `sql/sys_user_alter.sql` 重写：第1步 UPDATE 用旧列名 `avatar` 清字符串→0；第2步改用 `CHANGE COLUMN avatar avatar_id bigint NOT NULL DEFAULT 0`（改名+改类型一步到位）；含旧 URL→真实附件ID 可选迁移说明。
- JSON 契约变化：Java 字段 `avatarId` 经 Jackson 输出为 `avatarId`（camelCase），前端需从 `avatar` 改取 `avatarId`（值为数字附件ID）。
- 验证：admin `mvn -o -pl platform-admin -am compile` BUILD SUCCESS（仅无关 TreeBuilderComponent unchecked 提示）。

## 6. 本地存储静态映射（直链预览链接）
- 需求：用户要"直接访问即可预览"的链接，形态 `http://localhost:8080/file/2026/07/19/xxx.png`（本地存储；OSS/COS/MinIO 同理走各自域名）。先只做静态映射，头像字段（avatarUrl 透传）暂不动。
- 重要发现：静态映射**早已存在**——`platform-starter/.../file/FileUploadAutoConfiguration.java`（`@AutoConfiguration`，已在 AutoConfiguration.imports 第1个注册）用 yml `file.upload.url-prefix`(`/file/`) + `file.upload.path`(`./uploads`) 注册了 `/file/**` → `file:./uploads/` 的映射。即 `/file/...` 本就能直接取到图。
- 双层来源（已对齐，均为 `/file/` + `./uploads`）：
  - 静态映射 handler 路径：yml `file.upload.url-prefix` + `file.upload.path`（仅本地）。
  - 存库 `fileUrl` 字符串：`SysStorageConfig.domain` + `SysStorageConfig.base_path`，由 `LocalFileStorage.buildUrl` 拼成（domain 默认 `/file/` → 相对链接；改成完整域名 → 完整链接）。
- 本次改动（方案 B 静态映射）：
  - `FileUploadAutoConfiguration.addResourceHandlers`：把 `./uploads` 用 `new File(path).getAbsolutePath()` 绝对化（不依赖启动工作目录），并加 `.setCachePeriod(3600)`（为后续头像列表缓存铺路）。
  - `sql/platform_db.sql` 种子：`sys_storage_config.domain` 由 `/file/` 改为 `http://localhost:8080/file`，使新上传附件的 `fileUrl` 直接是完整可预览 URL。
  - 新增 `sql/sys_storage_config_domain_update.sql`：给"已运行库"用的 UPDATE（`WHERE storage_type=1 AND is_default=1`，domain→`http://localhost:8080/file`；附可选回填历史 `file_url` 的 REPLACE 语句）。
- 验证：`mvn -o -pl platform-admin -am compile` BUILD SUCCESS。浏览器直接开 `http://localhost:8080/file/2026/07/19/<uuid>.png` 应直接预览（host/port 须与后端实际一致）。
- 注意：`domain` 写死 host/port，换环境（生产/域名）需改 `sys_storage_config.domain`；这是"自定义访问域名/CDN"字段的用途。前端契约：`AttachmentVO.fileUrl` 已是完整直链，可直接 `<img>/<el-avatar :src>` 使用，无需再调 `/attachment/preview?id=`。

## 7. 附件预览链接公共方法 + VO 填充 previewUrl
- 需求：① 一个"直接返回附件预览链接"的公共方法；② 上传成功 VO 里要有预览链接；③ 附件列表每个 VO（仅限图片）要有预览链接。
- 新增 `platform-component/.../file/AttachmentUrlComponent.java`：
  - `String getPreviewUrl(Long attachmentId)` / `String getPreviewUrl(SysAttachment)`：返回可直接访问的预览链接，优先用已落库 `SysAttachment.fileUrl`，为空再按存储配置实时 `FileStorageManager.getAccessUrl(config, fileKey)` 拼接（兼容历史/失效数据）。
  - `boolean isImage(SysAttachment)` / `isImage(String contentType, String ext)`：判断图片（优先 content_type 前缀 `image/`，否则扩展名 jpg/jpeg/png/gif/bmp/webp）。
- `FileStorageManager` 新增便捷方法 `getAccessUrl(SysStorageConfig config, String fileKey)`（按配置路由到对应存储实现的 getAccessUrl）。
- `AttachmentVO` 新增 `previewUrl`（Schema 注明"预览链接(仅图片)"）；`fileUrl` 保留（always 有，为通用访问地址）。
- `SysAttachmentController`：注入 `AttachmentUrlComponent`；新增私有 `fillPreviewUrl(SysAttachment, AttachmentVO)`（仅图片时 `vo.setPreviewUrl(getPreviewUrl(att))`）；在 upload / page(convert) / view 三处调用。列表分页"仅限图片"通过 `isImage` 判定，非图片 previewUrl 留 null。
- 关键依赖事实：`platform-component` 依赖 platform-starter + platform-service，故组件可同时用 `FileStorageManager` 与 `SysStorageConfigService`/`SysAttachmentService`（starter 不能反向依赖 service，所以"按 configId 加载配置"放在 component 层而非 FileStorageManager）。
- 验证：`mvn -o -pl platform-admin -am compile` BUILD SUCCESS（仅无关 TreeBuilderComponent unchecked 提示）。
- 前端契约：图片用 `row.previewUrl`（或 `fileUrl`）直接 `<img>/<el-avatar>`；非图片 `previewUrl=null`，用 `fileUrl`/下载接口；avatarUrl 透传仍按计划后续做。

## 8. sys_attachment 删除 file_url 字段（URL 改为可派生，不落库）
- 决策：`file_url` 属可派生数据（= `sys_storage_config.domain` + `file_key`），存储易与 domain 不一致/随环境失效，故从 `sys_attachment` 移除，URL 全部运行时由 `AttachmentUrlComponent` 拼。
- 改动：
  - `SysAttachment` 实体删除 `fileUrl` 字段（`@TableField("file_url")`）。
  - `sql/platform_db.sql` 的 `sys_attachment` CREATE TABLE 删除 `file_url` 列；新增 `sql/sys_attachment_drop_file_url.sql`（运行库 `ALTER TABLE ... DROP COLUMN file_url`）。
  - `sys_storage_config_domain_update.sql`：移除"可选回填 file_url"的注释块（列已不存在），改为说明 URL 由 domain+fileKey 实时拼。
  - `SysAttachmentController.upload` 与 `FileChunkController.merge`：删除 `attach.setFileUrl(result.getFileUrl())`。
  - `AttachmentUrlComponent`：新增 `getAccessUrl(Long)` / `getAccessUrl(SysAttachment)`（= `storageConfigService.findById(configId)` + `fileStorageManager.getAccessUrl(config, fileKey)`）；`getPreviewUrl` 改为 `isImage ? getAccessUrl : null`（去掉读 fileUrl 分支）。
  - 两个 Controller 统一 `fillUrls(SysAttachment, AttachmentVO)`：设 `fileUrl`(全部) + `previewUrl`(仅图片)。`FileChunkController` 新增注入 `AttachmentUrlComponent` 并复用 `fillUrls`；`SysAttachmentController` 的 `fillPreviewUrl` 重命名为 `fillUrls`。
  - `FileUploadResult.fileUrl` DTO 字段保留（无害，仅不再落库）。
- 验证：`mvn -o -pl platform-admin -am compile` BUILD SUCCESS。
- 注意：运行库需执行 `sql/sys_attachment_drop_file_url.sql`；完整预览链接仍取决于 `sys_storage_config.domain`（本地已设为 `http://localhost:8080/file`）。

## changePassword 接入 RSA 加密
- 背景：登录(login)/二次认证(secondAuth)已无条件 RSA 解密；用户改密接口 `/user/changePassword` 原本收明文密码，本次补上 RSA。
- 改动：
  - `SysUserController`：新增注入 `RsaComponent`（`com.platform.starter.security.RsaComponent`）；`changePassword` 对 `oldPassword`/`newPassword`/`confirmPassword` **无条件** `rsaComponent.decryptByPrivateKey(...)`（与 login 一致，前端必须加密）。
  - 因 RSA/ECB/PKCS1Padding 随机填充，两次加密同明文密文不同 → 比对新/确认密码**改到解密后比对明文**；新密码长度校验(6~64)也移到解密后。
  - `UserPasswordVO.newPassword`：移除 `@Size(min=6,max=64)`（密文约344字符会被 Bean Validation 拦），`@Size` import 一并移除；明文长度约束改在 Controller 解密后 `Assert.isTrue`。
- 待确认：同一 `SysUserController.add`(第234行 `setPassword(passwordEncoder.encode(initPassword))`) 也收明文初始密码，是否同样需要 RSA 由 boss 决定（本次仅按指令改 changePassword）。
- 验证：`mvn -o -pl platform-admin -am compile` BUILD SUCCESS（仅 TreeBuilderComponent 无关 unchecked 提示）。
