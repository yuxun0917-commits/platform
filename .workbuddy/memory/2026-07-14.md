# 2026-07-14 工作日志

- 设计多文件存储可切换方案（策略模式 + sys_storage_config 配置驱动），确认 `storage_type` 用整型枚举码值、`sys_attachment` 不冗余 storage_type。
- 在 `sql/` 生成建表脚本：`sys_storage_config.sql`（存储配置表+本地默认种子）、`sys_attachment.sql`（附件表，含 config_id 关联、无 storage_type）。
- 下一步（未实施）：落地 StorageTypeEnum / FileStorage 接口与各实现 / FileStorageManager / 两张表的实体+Service+Controller，并把原 platform-starter 的 FileService 重构为 LocalFileStorage。
- 已落地多文件存储代码：platform-common 新增 StorageTypeEnum / StorageConfigStatusEnum；platform-starter 新增 FileStorage 接口 + FileUploadResult + 4 个实现（Local/Oss/Cos/Minio）+ 对应 Factory + FileStorageManager，删除旧 FileService，FileProcessComponent 改为校验后走 Manager；platform-service 扩展 SysStorageConfigService（findDefault/setDefault/paging/selectList）与 SysAttachmentService（paging/removeAttachment/countByConfigId）；platform-admin 新增 SysStorageConfigController（page/select-list/view/enums/add/edit/delete/set-default/edit-status）、SysAttachmentController（upload/page/view/delete，上传落库、删除同步物理文件）、VO（StorageConfigSave/Edit/VO、AttachmentVO）；返回密钥脱敏。SDK 依赖加入 starter 与根 pom dependencyManagement。编译通过（BUILD SUCCESS，全部 7 模块）。
- 用户要求四种存储策略分开放包：已将 Local/ Oss/ Cos/ Minio 各实现+Factory 移入 `com.platform.starter.file.{local,oss,cos,minio}` 子包，共享抽象（FileStorage/FileUploadResult/FileStorageFactory/FileStorageManager）留 `file` 根包；删除遗留死代码 FileService.java。重新编译通过（BUILD SUCCESS，全部 7 模块）。
- 新增分片上传（断点续传）：FileStorage 接口增加 `upload(File, originalFilename)` 重载并在 4 种实现中实现；FileUploadProperties 增加 `chunkTempDir`（默认 ./chunks）；platform-component 新增 ChunkUploadComponent（saveChunk/listChunks/isAllUploaded/merge/cleanup，分片落盘 `<identifier>/<n>.part`）；platform-admin 新增 FileChunkController（`POST /attachment/chunk/upload`、`GET /attachment/chunk/check`、`POST /attachment/chunk/merge`，合并后走存储并落 sys_attachment，结束清理临时文件）。支持 configId/bizType/bizId/contentType，类型校验走 allowedTypes，分片绕过单文件大小限制。编译通过（BUILD SUCCESS，全部 7 模块）。
- 完善根目录 `README.md`：核对真实项目结构（模块名 platform-*、包名 com.platform.*、sql/platform_db.sql、Sa-Token 1.45.0、MapperFacade 而非 MapStruct、6 个自动装配类、依赖方向、接口前缀 /user /storage-config /attachment/chunk /job 等），修正原草稿中 project-* 占位名、旧版本、MapStruct 等错误，新增"文件存储中心（多存储策略+分片上传）""定时任务（Quartz）"两章，补充启动步骤与接口示例。
