<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.platform.service.mapper.SysRoleMapper">

    <select id="listMenuByIds" resultType="com.platform.common.entity.admin.SysMenu">
        SELECT
            m.id,
            m.parent_id,
            m.menu_name,
            m.menu_type,
            m.path,
            m.component,
            m.redirect,
            m.icon,
            m.perms,
            m.display_order,
            m.is_hidden,
            m.is_cache,
            m.is_external,
            m.status
        FROM
            sys_menu m
            LEFT JOIN sys_role_menu rm ON rm.menu_id = m.id
        WHERE
            m.is_delete = 0
            AND rm.role_id IN
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
    </select>
</mapper>
